[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kmeans"
version = "0.0.1"
requires-python = ">=3.8"
dependencies = [
    "numpy",
    "boto3",
    "torch==1.4.0",
]
